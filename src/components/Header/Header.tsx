import { IonHeader } from "@ionic/react";
import { HeaderContent } from "./styled";

export default function Header() {
  return (
    <IonHeader>
      <HeaderContent>
        <h4>yol</h4>
      </HeaderContent>
    </IonHeader>
  );
}
